import { SharedWorld } from '../simulation/world.js';
const PROTO=3,MAX_PLAYERS=8,CLOCK_START=6,HOURS_PER_SEC=1/60,WORLD_LIMIT=500,MIN_STATE_MS=70;const W={clear:'晴れ',cloudy:'くもり',rain:'雨'};
export class MultiplayerRoom{constructor(ctx,env){this.ctx=ctx;this.env=env;this.players=new Map;this.world=new SharedWorld;this.loop=null;this.lastSnapshot=0;this.lastWeather=this.world.weather}
async fetch(r){const u=new URL(r.url);if(u.pathname==='/api/voice/join')return this._voiceJoin(r);if(r.headers.get('Upgrade')!=='websocket')return new Response('WebSocket expected',{status:426});const p=new WebSocketPair;this.ctx.acceptWebSocket(p[1]);return new Response(null,{status:101,webSocket:p[0]})}
async _cf(path,opt={}){const r=await fetch(`https://api.cloudflare.com/client/v4/accounts/${this.env.CLOUDFLARE_ACCOUNT_ID}/realtime/kit/${this.env.REALTIMEKIT_APP_ID}${path}`,{...opt,headers:{authorization:`Bearer ${this.env.REALTIMEKIT_API_TOKEN}`,'content-type':'application/json',...(opt.headers||{})}});const j=await r.json().catch(()=>({}));if(!r.ok||j.success===false)throw new Error(`RealtimeKit ${r.status}: ${JSON.stringify(j.errors||j)}`);return j}
async _voiceJoin(r){try{if(!this.env.REALTIMEKIT_API_TOKEN)return new Response('RealtimeKit secret missing',{status:503});const b=await r.json(),playerId=String(b.playerId||'').slice(0,36),name=String(b.name||'angler').slice(0,32);if(!playerId)return new Response('playerId required',{status:400});let meetingId=await this.ctx.storage.get('voiceMeetingId');if(!meetingId){const m=await this._cf('/meetings',{method:'POST',body:JSON.stringify({title:'Lakeside Fishing lake-1'})});meetingId=m.data?.id;if(!meetingId)throw new Error('meeting id missing');await this.ctx.storage.put('voiceMeetingId',meetingId)}let preset=await this.ctx.storage.get('voicePreset');if(!preset){const p=await this._cf('/presets?per_page=100');preset=p.data?.[0]?.name;if(!preset)throw new Error('RealtimeKit preset missing');await this.ctx.storage.put('voicePreset',preset)}const p=await this._cf(`/meetings/${meetingId}/participants`,{method:'POST',body:JSON.stringify({name,custom_participant_id:playerId,preset_name:preset})});return Response.json({token:p.data?.token,participantId:p.data?.id})}catch(e){console.error('[voice join]',e);return new Response(String(e.message||e),{status:502})}}
async _clock(){let e=await this.ctx.storage.get('clockEpoch');if(!e){e=Date.now();await this.ctx.storage.put('clockEpoch',e)}return(CLOCK_START+(Date.now()-e)/1000*HOURS_PER_SEC)%24}_restore(ws){let p=this.players.get(ws);if(p)return p;let a;try{a=ws.deserializeAttachment()}catch(e){}if(!a?.joined)return null;p={id:a.id,name:a.name,x:0,y:0,z:0,yaw:0,a:'idle',fresh:true,lastState:0,lastFight:0,lastVisual:0,lastChat:0,visual:null};this.players.set(ws,p);return p}_joined(){const o=[];for(const sock of this.ctx.getWebSockets()){const p=this._restore(sock);if(p)o.push({sock,p})}return o}_broadcast(o,except=null){const s=JSON.stringify(o);for(const{sock}of this._joined())if(sock!==except)try{sock.send(s)}catch(e){}}_system(text){this._broadcast({t:'system',text,ts:Date.now()})}_startLoop(){if(this.loop)return;this.loop=setInterval(()=>{const j=this._joined();if(!j.length){clearInterval(this.loop);this.loop=null;return}this.world.tick(j.map(x=>x.p));if(this.world.weather!==this.lastWeather){this.lastWeather=this.world.weather;this._broadcast({t:'weather',weather:this.world.weather});this._system(`天候が「${W[this.world.weather]||this.world.weather}」に変わった`)}const n=Date.now();if(n-this.lastSnapshot>=100){this.lastSnapshot=n;this._broadcast({t:'fish_snapshot',fish:this.world.snapshot()})}},100)}async webSocketMessage(ws,raw){let m;try{m=JSON.parse(raw)}catch(e){return}if(!m||typeof m!=='object')return;if(m.t==='join'){if(m.v!==PROTO){ws.send(JSON.stringify({t:'error',code:'version'}));ws.close(4000,'version');return}if(this._joined().length>=MAX_PLAYERS){ws.send(JSON.stringify({t:'error',code:'full'}));ws.close(4001,'full');return}const id=crypto.randomUUID().slice(0,8),name=String(m.name||'').slice(0,12)||'angler',p={id,name,x:0,y:0,z:0,yaw:0,a:'idle',fresh:true,lastState:0,lastFight:0,lastVisual:0,lastChat:0,visual:null},j=this._joined(),players=j.map(x=>({id:x.p.id,name:x.p.name,x:x.p.x,y:x.p.y,z:x.p.z,yaw:x.p.yaw,a:x.p.a})),visuals=j.filter(x=>x.p.visual).map(x=>({id:x.p.id,...x.p.visual}));this.players.set(ws,p);ws.serializeAttachment({joined:true,id,name});ws.send(JSON.stringify({t:'welcome',id,clock:await this._clock(),weather:this.world.weather,players,visuals,fish:this.world.snapshot()}));this._broadcast({t:'join',id,name,x:0,y:0,z:0,yaw:0,a:'idle'},ws);this._startLoop();return}const p=this._restore(ws);if(!p)return;const now=Date.now();if(m.t==='chat'){if(now-p.lastChat<500)return;p.lastChat=now;const text=String(m.text||'').replace(/[\r\n\t]+/g,' ').trim().slice(0,120);if(text)this._broadcast({t:'chat',id:p.id,name:p.name,text,ts:now});return}if(m.t==='s'){if(now-p.lastState<MIN_STATE_MS)return;p.lastState=now;const x=+m.x,y=+m.y,z=+m.z,yaw=+m.yaw;if(![x,y,z,yaw].every(Number.isFinite)||Math.abs(x)>WORLD_LIMIT||Math.abs(z)>WORLD_LIMIT||Math.abs(y)>100)return;Object.assign(p,{x,y,z,yaw,a:typeof m.a==='string'?m.a.slice(0,12):'idle',fresh:false});this._broadcast({t:'s',id:p.id,x,y,z,yaw,a:p.a},ws);return}if(m.t==='v'){if(now-p.lastVisual<MIN_STATE_MS)return;p.lastVisual=now;const bx=+m.bx,by=+m.by,bz=+m.bz;if(![bx,by,bz].every(Number.isFinite))return;const v={fs:String(m.fs||'idle').slice(0,12),charge:c(+m.charge),tension:c(+m.tension),reeling:c(+m.reeling),rod:String(m.rod||'bamboo').slice(0,16),bait:String(m.bait||'worm').slice(0,16),rarity:Math.max(0,Math.min(5,+m.rarity||0)),bx,by,bz,line:!!m.line};p.visual=v;this._broadcast({t:'v',id:p.id,...v},ws);return}if(m.t==='bait'){this.world.setBait(p.id,m);return}if(m.t==='bait_clear'){this.world.setBait(p.id,null);return}if(m.t==='hook'){if(this.world.hook(p.id,String(m.fishId||'')))this._broadcast({t:'fish_hooked',fishId:m.fishId,playerId:p.id});return}if(m.t==='fight'){if(now-p.lastFight<MIN_STATE_MS)return;p.lastFight=now;this.world.fightUpdate(p.id,String(m.fishId||''),m);return}if(m.t==='fight_end'){const id=String(m.fishId||''),result=m.result==='caught'?'caught':'escaped',f=this.world.fishes.get(id),caught=f?{speciesId:f.speciesId,length:f.length}:null,e=this.world.endFight(p.id,id,result);if(e){this._broadcast({t:e.removed?'fish_caught':'fish_escaped',fishId:id,playerId:p.id});if(e.removed&&caught)this._system(`${p.name} が ${caught.length.toFixed(1)}cm の ${fishName(caught.speciesId)} を釣り上げた`)}}}webSocketClose(ws){this._drop(ws)}webSocketError(ws){this._drop(ws)}_drop(ws){const p=this._restore(ws);this.players.delete(ws);try{ws.close()}catch(e){}if(p){this.world.dropPlayer(p.id);this._broadcast({t:'leave',id:p.id,name:p.name},ws)}}}const c=v=>Number.isFinite(v)?Math.max(0,Math.min(1,v)):0;const N={rainbow_trout:'ニジマス',brown_trout:'ブラウントラウト',char:'イワナ',yamame:'ヤマメ',bluegill:'ブルーギル',black_bass:'ブラックバス',carp:'コイ',crucian_carp:'フナ',catfish:'ナマズ',smelt:'ワカサギ',sweetfish:'アユ',eel:'ウナギ',salmon:'サケ',huchen:'イトウ'};const fishName=id=>N[id]||id;
