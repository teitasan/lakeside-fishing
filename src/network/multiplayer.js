const PROTO=3,SEND_HZ=10;export const MULTIPLAYER_SEED=123456789,MP_SESSION_KEY='lakeside-fishing-mp-join',MP_NAME_KEY='lakeside-fishing-mp-name';export class MultiplayerClient{constructor(){this.ws=null;this.id=null;this.connected=false;this.name='';this.onWelcome=this.onJoin=this.onLeave=this.onState=this.onVisual=this.onFishSnapshot=this.onFishReserved=this.onFishHooked=this.onFishHookRejected=this.onFishEscaped=this.onFishCaught=this.onWeather=this.onChat=this.onSystem=this.onClose=this.onError=null;this._lastSendAt=0;this._lastSent='';this._lastVisualAt=0;this._lastVisual='';this._reconnectTimer=null;this._closedByUser=false;this._reconnecting=false}connect(name){this.name=name||this.name||'angler';this._closedByUser=false;this._reconnecting=!!this.id;const proto=location.protocol==='https:'?'wss:':'ws:';let ws;try{ws=new WebSocket(`${proto}//${location.host}/ws`)}catch(e){this._scheduleReconnect();return}this.ws=ws;ws.onopen=()=>ws.send(JSON.stringify({t:'join',v:PROTO,name:this.name}));ws.onmessage=ev=>{let m;try{m=JSON.parse(ev.data)}catch(e){return}const h={welcome:this.onWelcome,join:this.onJoin,leave:this.onLeave,s:this.onState,v:this.onVisual,fish_reserved:this.onFishReserved,fish_hooked:this.onFishHooked,fish_hook_rejected:this.onFishHookRejected,fish_escaped:this.onFishEscaped,fish_caught:this.onFishCaught,weather:this.onWeather,chat:this.onChat,system:this.onSystem,error:this.onError};if(m.t==='welcome'){this.connected=true;this.id=m.id}if(m.t==='fish_snapshot')this.onFishSnapshot?.(m.fish||[]);else if(m.t==='weather')this.onWeather?.(m.weather);else h[m.t]?.(m)};ws.onclose=()=>{const was=this.connected;this.connected=false;if(was)this.onClose?.();if(!this._closedByUser)this._scheduleReconnect()};ws.onerror=()=>{if(!this.connected)this.onError?.('connect')}}_scheduleReconnect(){if(this._closedByUser||this._reconnectTimer)return;this._reconnectTimer=setTimeout(()=>{this._reconnectTimer=null;if(!this._closedByUser)this.connect(this.name)},1500)}_send(o){if(!this.connected||!this.ws||this.ws.readyState!==1)return false;this.ws.send(JSON.stringify(o));return true}sendChat(text){const s=String(text||'').trim().slice(0,120);if(s)this._send({t:'chat',text:s})}sendState(x,y,z,yaw,a){if(!this.connected)return;const now=performance.now();if(now-this._lastSendAt<1000/SEND_HZ)return;const s={t:'s',x:r(x),y:r(y),z:r(z),yaw:Math.round(yaw*1000)/1000,a},key=`${s.x},${s.y},${s.z},${s.yaw},${s.a}`;if(key===this._lastSent&&now-this._lastSendAt<1000)return;this._lastSendAt=now;this._lastSent=key;this._send(s)}sendVisual(v){if(!this.connected||!v)return;const now=performance.now();if(now-this._lastVisualAt<1000/SEND_HZ)return;const m={t:'v',fs:String(v.fs||'idle').slice(0,12),charge:r(v.charge||0),tension:r(v.tension||0),reeling:r(v.reeling||0),rod:String(v.rod||'bamboo').slice(0,16),bait:String(v.bait||'worm').slice(0,16),rarity:Math.max(0,Math.min(5,v.rarity|0)),bx:r(v.bx||0),by:r(v.by||0),bz:r(v.bz||0),line:!!v.line},key=JSON.stringify(m);if(key===this._lastVisual&&now-this._lastVisualAt<1000)return;this._lastVisualAt=now;this._lastVisual=key;this._send(m)}setBait(b){this._send({t:'bait',...b})}clearBait(){this._send({t:'bait_clear'})}hookFish(f){this._send({t:'hook',fishId:f})}fightUpdate(f,x,y,z){this._send({t:'fight',fishId:f,x:r(x),y:r(y),z:r(z)})}endFight(f,result){this._send({t:'fight_end',fishId:f,result})}close(){this._closedByUser=true;this.connected=false;if(this._reconnectTimer)clearTimeout(this._reconnectTimer);try{this.ws?.close()}catch(e){}}}const r=v=>Math.round(v*100)/100;
